name: Ansible Lint

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Исправленное название экшена
      - name: Run ansible-lint
        uses: ansible/ansible-lint@main
